name: python

on:
  push:
  pull_request:

jobs:
  offline:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m pip install --upgrade pytest
      - run: python -m pytest -q
      - run: python -m compileall -q spirebrain start.py run_agent.py
      - run: python tools/check_contracts.py
      # A clean checkout has no runtime logs.  Use the committed synthetic
      # trace to exercise the metrics contract without pretending it is a
      # real-game evaluation.
      - run: python -m spirebrain.analysis.metrics --input tests/fixtures/metrics/decision_trace.jsonl
